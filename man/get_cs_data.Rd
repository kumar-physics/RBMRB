% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/chemical_shift_statistics.R
\name{get_cs_data}
\alias{get_cs_data}
\title{Fetch chemical shift data from the BMRB database}
\usage{
get_cs_data(
  residue = "*",
  atom = "*",
  filtered = TRUE,
  sd_limit = 10,
  ambiguity = "*",
  ph_min = NULL,
  ph_max = NULL,
  t_min = NULL,
  t_max = NULL,
  standard_amino_acids = TRUE,
  database = "macromolecules"
)
}
\arguments{
\item{residue}{Residue in IUPAC 3-letter format (e.g. "ALA"). "*" = any.}

\item{atom}{Atom name (e.g. "CA", "HB*"). "*" = any.}

\item{filtered}{Remove outliers beyond sd_limit * SD. Default TRUE.}

\item{sd_limit}{SD factor for outlier removal. Default 10.}

\item{ambiguity}{Ambiguity code filter ("*" = no filter; 1 = unambiguous).}

\item{ph_min, ph_max}{pH range filter (optional).}

\item{t_min, t_max}{Temperature range in Kelvin (optional).}

\item{standard_amino_acids}{Restrict to standard amino acids. Default TRUE.}

\item{database}{"macromolecules" (default) or "metabolomics".}
}
\value{
A data.frame with columns Val, Comp_ID, Atom_ID, Ambiguity_code,
  Entry_ID, Comp_index_ID, pH, Temperature_K and others.
}
\description{
Downloads chemical shifts for the given residue and atom combination from
the BMRB database. Mirrors `pybmrb.ChemicalShiftStatistics.get_data()`.
}
\examples{
\dontrun{
  df <- get_cs_data("ALA", "CA")
  df <- get_cs_data("*", "HB*")
}
}
