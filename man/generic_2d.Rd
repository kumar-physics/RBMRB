% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spectra.R
\name{generic_2d}
\alias{generic_2d}
\title{Plot a generic 2D NMR correlation spectrum}
\usage{
generic_2d(
  atom_x,
  atom_y,
  bmrb_ids = NULL,
  input_file_names = NULL,
  auth_tag = FALSE,
  legend = NULL,
  draw_trace = FALSE,
  peak_list = NULL,
  include_preceding = FALSE,
  include_next = FALSE,
  seq_walk = FALSE,
  full_walk = FALSE,
  output_format = "html",
  output_file = NULL,
  output_image_width = 800,
  output_image_height = 600,
  show_visualization = TRUE
)
}
\arguments{
\item{atom_x}{Atom name for the X axis (IUPAC, e.g. `"N"`, `"CA"`).}

\item{atom_y}{Atom name for the Y axis.}

\item{bmrb_ids}{BMRB entry ID(s) — integer, character, or vector.}

\item{input_file_names}{Path(s) to local NMR-STAR `.str` files.}

\item{auth_tag}{Use `Auth_seq_ID` for sequence numbering.}

\item{legend}{`NULL` (default), `"residue"`, or `"dataset"`.}

\item{draw_trace}{Draw lines connecting same-residue peaks across
datasets.}

\item{peak_list}{Path to a 2-column CSV file of unassigned peaks
(`H_ppm`, `N_ppm`) to overlay.}

\item{include_preceding}{Include chemical shifts from the preceding (i−1)
residue on the Y axis. Default `FALSE`.}

\item{include_next}{Include chemical shifts from the following (i+1)
residue on the Y axis. Default `FALSE`.}

\item{seq_walk}{Draw a trace connecting sequential i→i±1 pairs for
continuous sequence segments. Default `FALSE`.}

\item{full_walk}{Like `seq_walk` but ignores missing residues. Default `FALSE`.}

\item{output_format}{`"html"` (default), `"png"`, `"jpg"`, `"pdf"`,
`"svg"`, or `"webp"`.}

\item{output_file}{File path to save the visualization (optional).}

\item{output_image_width, output_image_height}{Image dimensions in pixels.}

\item{show_visualization}{Open in browser / display inline. Default `TRUE`.}
}
\value{
Invisibly, the peak list.
}
\description{
Generates a 2D spectrum for any two atom types. Supports sequence-walk
tracing, preceding/next-residue peaks, and all the same overlay options as
the other spectrum functions.
Equivalent to `pybmrb.Spectra.generic_2d()`.
}
\examples{
\dontrun{
generic_2d("N", "CA", bmrb_ids = 15000, legend = "residue")
generic_2d("N", "CA", bmrb_ids = 15000, include_next = TRUE,
           seq_walk = TRUE)
}
}
