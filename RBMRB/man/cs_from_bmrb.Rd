% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/chemical_shift.R
\name{cs_from_bmrb}
\alias{cs_from_bmrb}
\title{Extract chemical shifts from BMRB database entries}
\usage{
cs_from_bmrb(bmrb_ids, auth_tag = FALSE)
}
\arguments{
\item{bmrb_ids}{A single BMRB entry ID or a vector of IDs.}

\item{auth_tag}{Logical. Use `Auth_seq_ID` for sequence numbering. Default FALSE.}
}
\value{
A nested named list:
  `list(dataset_id = list(chain = list(seq_id = list(atom = cs_value),
  seq_ids = integer_vector)))`.
}
\description{
Fetches assigned chemical shift data for one or more BMRB entries.
Uses the same zlib-compressed JSON endpoint as PyBMRB / pynmrstar.
Mirrors `pybmrb.ChemicalShift.from_bmrb()`.
}
\examples{
\dontrun{
  cs <- cs_from_bmrb(15060)
  cs_peek(cs)
  cs_from_bmrb(c(17076, 17077))
}
}
