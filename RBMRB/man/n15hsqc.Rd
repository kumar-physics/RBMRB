% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spectra.R
\name{n15hsqc}
\alias{n15hsqc}
\title{Simulate a \ifelse{html}{\out{<sup>1</sup>H-<sup>15</sup>N HSQC}}{\eqn{^{1}H{-}^{15}N} HSQC} spectrum}
\usage{
n15hsqc(
  bmrb_ids = NULL,
  input_file_names = NULL,
  auth_tag = FALSE,
  legend = NULL,
  draw_trace = FALSE,
  include_sidechain = TRUE,
  peak_list = NULL,
  output_format = "html",
  output_file = NULL,
  output_image_width = 800,
  output_image_height = 600,
  show_visualization = TRUE
)
}
\arguments{
\item{bmrb_ids}{BMRB entry ID(s) -- integer, character, or vector.}

\item{input_file_names}{Path(s) to local NMR-STAR `.str` files.}

\item{auth_tag}{Use `Auth_seq_ID` for sequence numbering.}

\item{legend}{`NULL` (default), `"residue"`, or `"dataset"`.}

\item{draw_trace}{Draw lines connecting same-residue peaks across
datasets.}

\item{include_sidechain}{Include sidechain NH2 peaks (ASN/GLN/ARG/TRP/HIS).}

\item{peak_list}{Path to a 2-column CSV file of unassigned peaks
(`H_ppm`, `N_ppm`) to overlay.}

\item{output_format}{`"html"` (default), `"png"`, `"jpg"`, `"pdf"`,
`"svg"`, or `"webp"`.}

\item{output_file}{File path to save the visualization (optional).}

\item{output_image_width, output_image_height}{Image dimensions in pixels.}

\item{show_visualization}{Open in browser / display inline. Default `TRUE`.}
}
\value{
Invisibly, a list with elements `x` (H ppm), `y` (N ppm),
  `dataset`, `residue`, `info`, and (when `draw_trace = TRUE`) `cs_track`.
}
\description{
Plots an interactive 1H-15N HSQC spectrum for one or more BMRB entries
and/or NMR-STAR files. Overlay multiple datasets as scatter plots; optionally
draw trace lines connecting peaks from the same residue position across
datasets. Equivalent to `pybmrb.Spectra.n15hsqc()`.
}
\examples{
\dontrun{
# Single entry
n15hsqc(bmrb_ids = 15060, legend = "residue")

# Compare two entries with trace lines
n15hsqc(bmrb_ids = c(17076, 17077), legend = "dataset", draw_trace = TRUE)

# Overlay user's own NMR-STAR file with BMRB entry
n15hsqc(bmrb_ids = 15060, input_file_names = "MyData.str",
        legend = "dataset")
}
}
